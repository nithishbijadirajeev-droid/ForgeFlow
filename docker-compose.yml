services:
  forgeflow:
    build:
      context: .
      dockerfile: app/Dockerfile

    container_name: forgeflow

    ports:
      - "8080:8080"

    environment:
      SERVER_HOST: 0.0.0.0
      SERVER_PORT: 8080
      APP_NAME: ForgeFlow
      APP_VERSION: v0.1.0

    restart: unless-stopped